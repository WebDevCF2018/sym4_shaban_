<form method="post" action="{{ path('instruments_delete', {'idinstruments': instrument.idinstruments}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instrument.idinstruments) }}">
    <button class="btn">Delete</button>
</form>
