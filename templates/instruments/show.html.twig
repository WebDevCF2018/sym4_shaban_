{% extends 'base.html.twig' %}

{% block title %}Instruments{% endblock %}

{% block body %}
    <h1>Instruments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idinstruments</th>
                <td>{{ instrument.idinstruments }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ instrument.intitule }}</td>
            </tr>
            <tr>
                <th>Ladesc</th>
                <td>{{ instrument.ladesc }}</td>
            </tr>
            <tr>
                <th>Parution</th>
                <td>{{ instrument.parution ? instrument.parution|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instruments_index') }}">back to list</a>

    <a href="{{ path('instruments_edit', {'idinstruments': instrument.idinstruments}) }}">edit</a>

    {{ include('instruments/_delete_form.html.twig') }}
{% endblock %}
